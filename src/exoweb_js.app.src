%% -*- erlang -*-
{application, exoweb_js,
 [{description, "Exodm web interface."},
  {vsn, git},
  {registered, [exoweb]},
  {applications, [
                  kernel,
                  stdlib,
		  sasl,
		  gettext,
		  gen_smtp,
		  yaws,
		  wse,
		  exo,
		  exodmapi,
		  sync,
		  lager
                 ]},
  {modules,[exoweb, exoweb_sup]},
  {mod, {exoweb, []}}
]}.
