%% -*- erlang -*-
{application, exoweb_js,
 [{description, "Exodm web interface."},
  {vsn, git},
  {registered, [exoweb]},
  {applications, [
                  kernel,
                  stdlib,
		  sasl,
		  gen_smtp,
		  yaws,
		  wse,
		  exo,
		  exodmapi,
		  lager
                 ]},
  {modules,[exoweb, exoweb_sup]},
  {mod, {exoweb, []}}
]}.
