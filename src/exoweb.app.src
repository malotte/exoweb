%% -*- erlang -*-
{application, exoweb,
 [{description, "Exodm web interface."},
  {vsn, git},
  {registered, [exoweb]},
  {applications, [
                  kernel,
                  stdlib,
		  sasl,
		  gen_smtp,
		  lager
                 ]},
  {modules,[exoweb, exoweb_sup]},
  {mod, {exoweb, []}}
]}.
